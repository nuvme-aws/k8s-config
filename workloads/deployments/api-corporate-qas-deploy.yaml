---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-corporate-qas
  namespace: oficina5
  labels:
    app: api-corporate-qas
  annotations:
    # Container Image Automated Updates
    flux.weave.works/automated: "true"
    # do not apply this manifest on the cluster
    #flux.weave.works/ignore: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-corporate-qas
  template:
    metadata:
      labels:
        app: api-corporate-qas
    spec:
      containers:
      - name: api-corporate-qas
        image: 837042927026.dkr.ecr.us-east-2.amazonaws.com/corporate-qas:0f64ce57f239c4352f41122229619aa9ca7d8f9e
        env:
        - name: CONN_DB
          valueFrom:
            secretKeyRef:
              name: api-corporate-qas
              key: CONN_DB
        - name: CONN_HOST
          valueFrom:
            secretKeyRef:
              name: api-corporate-qas
              key: CONN_HOST
        - name: CONN_PASS
          valueFrom:
            secretKeyRef:
              name: api-corporate-qas
              key: CONN_PASS
        - name: CONN_USER
          valueFrom:
            secretKeyRef:
              name: api-corporate-qas
              key: CONN_USER
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: http
        readinessProbe:
          httpGet:
            path: /
            port: http
