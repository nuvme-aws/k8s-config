---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: metodista-aa
  namespace: oficina5
  labels:
    app: metodista-aa
  annotations:
    # Container Image Automated Updates
    flux.weave.works/automated: "true"
    # do not apply this manifest on the cluster
    #flux.weave.works/ignore: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metodista-aa
  template:
    metadata:
      labels:
        app: metodista-aa
    spec:
      containers:
      - name: metodista-aa
        image: 837042927026.dkr.ecr.us-east-2.amazonaws.com/metodista-qa
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: http
        readinessProbe:
          httpGet:
            path: /
            port: http
