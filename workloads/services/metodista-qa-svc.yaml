apiVersion: v1
kind: Service
metadata:
  name: metodista-qa
  namespace: oficina5
  labels:
    app: metodista-qa
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: metodista-qa
    #app.kubernetes.io/name: metodista-qa
